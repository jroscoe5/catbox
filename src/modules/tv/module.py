from modules.base_module import BaseModule

class TVModule(BaseModule):
    """
    """
    def __init__(self) -> None:
        super().__init__()

    def register(self, emitter) -> None:
        return super().register(emitter)
